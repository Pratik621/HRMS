{
  "name": "hrms-b2b",
  "version": "1.0.0",
  "private": true,
  "description": "HRMS monorepo — frontend (Vite static build) + backend (Express, deployed as a Vercel Serverless Function via /api/index.js)",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "install:all": "cd frontend && npm install && cd ../backend && npm install",
    "postinstall": "npm run install:all",
    "dev": "concurrently -n FRONTEND,BACKEND -c blue,green \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
